import React, { useState, useEffect } from 'react';

import { saveNumberToStorage, getNumberFromStorage } from '../utils';
import profiles from '../utils/data.json';

import Card from './Card';

export default function Carousel(): JSX.Element {
  return (
    <div>
      <h1>Carousel</h1>
    </div>
  );
}
